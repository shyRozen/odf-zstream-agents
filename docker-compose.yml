services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: zstream
      POSTGRES_USER: zstream
      POSTGRES_PASSWORD: zstream
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
